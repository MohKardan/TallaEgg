using System.ComponentModel;

namespace TallaEgg.Core.Enums.Order
{
    /// <summary>
    /// The market an order is placed in.
    ///
    /// فعلاً فقط نقدی. مقدار Futures حذف شد چون هرگز ست نمی‌شد ولی موتور تطبیق هم آن را
    /// نادیده می‌گرفت: یک سفارش آتی بی‌صدا با سفارش نقدی تطبیق می‌خورد. یک مقدار enum که
    /// استفاده نمی‌شود «برنامهٔ آینده» را مستند نمی‌کند — فقط یک تلهٔ اجرایی می‌سازد،
    /// همان‌طور که OrderRole.Taker ساخت.
    ///
    /// نکته برای آینده: معاملات <b>اعتباری/مارجین</b> اینجا جایی ندارد. مارجین یک بازار
    /// نیست، یک روش تأمین مالی است؛ سفارش اعتباری و نقدی باید در یک دفتر با هم تطبیق
    /// بخورند. اگر روزی لازم شد ثبت شود، فیلد جداگانه‌ای روی محور «تأمین مالی» می‌خواهد.
    ///
    /// معاملات آتی واقعی (تحویل در سررسید، قیمت مستقل) یک ابزار متفاوت است و آن‌وقت
    /// دفتر سفارش جدا هم می‌خواهد. در ROADMAP ثبت می‌شود، نه اینجا.
    /// </summary>
    public enum TradingType
    {
        [Description("معاملات نقدی")]
        Spot
    }
}
